WebLLM 0.2.85 (https://github.com/mlc-ai/web-llm), Apache-2.0.
Self-contained ES module copied unmodified from the published package, so the
site needs no CDN or build step. Model weights download from Hugging Face on
first use and are cached by the browser.
