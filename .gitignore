# OS / editor
.DS_Store
Thumbs.db
*.log

# Secrets never belong in this repo (they live in Cloudflare / Supabase / the stores)
.env
.env.*
*.keystore
*.jks
*.p12
*.p8
*.mobileprovision
signing-key-info.txt

# Native wrapper build output (if you keep a wrapper project here)
native/**/build/
native/**/.gradle/
native/**/DerivedData/

# Legacy Node/Expo version (server/, mobile/) — not used by the static app
node_modules/
dist/
.expo/
.expo-shared/
web-build/
*.db
*.db-journal
uploads/
ios/
android/
